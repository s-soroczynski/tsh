import React from 'react'

import { Typography } from '@mui/material';

export const Logo = () => {
  return (
    <Typography
      component="h2"
      sx={{
        fontSize: 24,
        fontWeight: '600'
      }}
    >
      join.tsh.io
    </Typography>
  )
}
